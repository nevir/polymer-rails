Description:
    Initializes your Rails project to make use of polymer-rails.

Example:
    rails generate polymer:init

    Or, if you want to be brave and let polymer-rails reorganize and clean up
    your assets, try:

    rails generate polymer:init --combine-manifests --no-default-js --no-turbolinks
